name: b

on:
  workflow_call:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for fail
        run: |
          if grep -q "fail" b/text.txt shared/text.txt 2>/dev/null; then
            echo "Found 'fail' in b/text.txt or shared/text.txt"
            exit 1
          fi
          echo "b is OK!"
