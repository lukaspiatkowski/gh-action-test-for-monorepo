name: a

on:
  workflow_call:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for fail
        run: |
          if grep -q "fail" a/text.txt shared/text.txt 2>/dev/null; then
            echo "Found 'fail' in a/text.txt or shared/text.txt"
            exit 1
          fi
          echo "a is OK!"
